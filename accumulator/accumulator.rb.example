def accumulator(reducer, initial_value)
  # Your solution
end

if $PROGRAM_NAME == __FILE__
  # Code written here or in other files will be ignored by the automated tests.
  # You can run this code when running the file with `ruby accumulator.rb`
end
